/**
 * Shared horizontal shells.
 * - wide: browsing + dashboards (1200)
 * - task: single decision flows (pay / lead / booking action)
 */
export const shellWide = "mx-auto w-full max-w-[1200px]";
export const shellTask = "mx-auto w-full max-w-[640px]";
