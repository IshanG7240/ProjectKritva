// Aggregated re-exports for @kritva/notifications.
//
// Per-channel adapters (sms, email, push), the preference-aware dispatcher,
// and React Email templates are populated in T-027 / T-028.

export {};
